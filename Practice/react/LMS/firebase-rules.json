{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "students": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "teachers": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "classes": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "subjects": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "syllabus": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "fees": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}
